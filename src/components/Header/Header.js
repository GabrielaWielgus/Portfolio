import React from "react";
import { Helmet } from "react-helmet";

const Header = () => {
    return(
        <div>
            <Helmet>
                <title>Portfolio - Gabriela Wielgus</title>
            </Helmet>
        </div>
    );
};

export default Header;